export async function compress(file,max=900,q=.62){return new Promise((ok,no)=>{const im=new Image(),url=URL.createObjectURL(file);im.onload=()=>{let s=Math.min(1,max/Math.max(im.width,im.height)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(im.width*s));c.height=Math.max(1,Math.round(im.height*s));c.getContext('2d',{alpha:false}).drawImage(im,0,0,c.width,c.height);URL.revokeObjectURL(url);ok(c.toDataURL('image/jpeg',q))};im.onerror=error=>{URL.revokeObjectURL(url);no(error)};im.src=url})}
