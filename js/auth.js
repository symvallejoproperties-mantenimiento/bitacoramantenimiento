import {DB} from './storage.js';
export async function requireAuth(role){await DB.seed();const session=DB.read(DB.keys.session,null);if(!session&&!role)return{id:'public',nombre:'Captura pública',rol:'public'};if(!session){location.href='login.html';return null}const user=DB.users().find(item=>item.id===session.id&&item.activo);if(!user){localStorage.removeItem(DB.keys.session);location.href='login.html';return null}const current={id:user.id,nombre:user.nombre,usuario:user.usuario,rol:user.rol};DB.write(DB.keys.session,current);if(role&&current.rol!==role){location.href=current.rol==='admin'?'admin.html':'captura.html';return null}return current}
export function logout(){localStorage.removeItem(DB.keys.session);location.href='login.html'}
export function bindShell(){document.querySelectorAll('[data-logout]').forEach(b=>b.onclick=logout);const t=document.querySelector('[data-theme]');if(t)t.onclick=()=>{let d=document.documentElement,s=DB.settings();s.theme=d.dataset.theme=d.dataset.theme==='dark'?'light':'dark';DB.write(DB.keys.settings,s)};document.documentElement.dataset.theme=DB.settings().theme||'light'}
